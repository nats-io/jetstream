{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nats.io/schemas/jetstream/api/v1/meta_peer_remove_response.json",
  "description": "A response from the JetStream $JS.API.SERVER.REMOVE API",
  "title": "io.nats.jetstream.api.v1.meta_peer_remove_response",
  "type": "object",
  "required": ["server"],
  "properties": {
    "server": {
      "type": "string",
      "description": "The ID of the server to remove from the meta group",
      "minimum": 1
    }
  }
}
